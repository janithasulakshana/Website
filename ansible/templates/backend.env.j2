NODE_ENV=production
PORT={{ backend_port }}
HOST=0.0.0.0
CORS_ORIGIN={{ cors_origin }}
JWT_SECRET={{ jwt_secret }}
DATABASE_PATH=/data/bookings.db
LOG_LEVEL={{ log_level }}
LOG_FORMAT={{ log_format }}
